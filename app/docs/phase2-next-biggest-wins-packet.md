# Phase 2 Next Biggest Wins Packet

This packet gives Manager AI one compact planning surface for the next three high-value controlled-agent handoffs per lane without reopening long progress threads or older source-batch docs.

Use it to answer:

- what the next three biggest safe wins are per controlled agent
- whether the next step is a source build, workflow hardening pass, consumer follow-on, or governance sweep
- what caveats must survive the handoff
- which items are blocked by validation risk versus implementation risk

Status rule:

- [source-assignment-board.md](/C:/Users/mike/11Writer/app/docs/source-assignment-board.md) remains the source-status truth.
- [source-validation-status.md](/C:/Users/mike/11Writer/app/docs/source-validation-status.md) remains the validation-traceability truth.
- Atlas and Wonder remain peer or user-directed input only.
- Source Discovery runtime, Wave LLM runtime, Browser Use guidance, Atlas Source Discovery Ten-Step notes, and Wonder macOS/plugin/connector planning are planning and runtime-boundary input here, not source-validation proof.
- The `2026-05-05 19:41 America/Chicago` source-expansion wave has now materially landed across Data, Geospatial, Marine, Aerospace, and Features/Webcam.

## Global Rules

- Keep `implemented`, `contract-tested`, `workflow-validated`, and `fully validated` distinct.
- Do not treat helper packages, review queues, runtime status endpoints, scheduler compatibility helpers, Browser Use guidance, or peer research as source-validation proof.
- Keep source-health, evidence basis, caveats, and no-harmful-use boundaries explicit in every handoff.
- Prefer bounded follow-on work over opening multiple fresh lanes when one lane is already partially implemented.

## Feature Occupancy Notes

- `Evidence Timeline` is partially occupied already through Aerospace's implemented evidence-timeline package and Atlas's peer/user-directed analyst-workbench timeline surface; future work should target cross-domain normalization only.
- `Review Packet Export` is partially occupied already through Geospatial Canada export packages, Marine report/export helpers, Aerospace review/export packages, and Features/Webcam source-ops export surfaces; future work should unify contract/caveat structure rather than add renamed per-domain packet helpers.
- `Source Candidate Intake Queue` is partially occupied already through Gather routing/governance docs, Features/Webcam candidate review-priority surfaces, and Source Discovery/Atlas/Wonder candidate-routing input; future work should be a cross-lane governance contract only.
- `Source Health Console` is partially occupied already through Geospatial family review/export, Marine source-health workflows, Aerospace source-readiness/context-availability packages, and Data AI methodology-aware source-health summaries; future work should aggregate those domain surfaces rather than rebuild them.
- `Attention Queue Unifier` is partially occupied already through Marine attention queues, Aerospace context review queues, Geospatial environmental review queues, Features/Webcam review-priority surfaces, and Data AI family/source review metadata; future work should normalize shared contracts rather than create another queue concept.
- `Source Fusion Snapshot` remains open globally, but it should compose the existing domain packages instead of replacing them or treating the feature as blank.

## Connect AI

| Rank | Source or workflow id | First safe slice | Why it matters now | Source or evidence caveats | Validation or status risk | Handoff type | Do-not-do boundary |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | `connect-shared-reporting-loop-contract-wave` | keep the active `19:15` shared reporting-loop contract and compatibility checkpoint bounded while it is still in progress | The completed `18:49` checkpoint has already rolled forward into a new shared-contract wave, so the highest-value Connect move is to keep that active lane honest. | Validation truth must stay machine-specific where Browser or Playwright launch is host-bound. | `medium` because stale checkpoint wording can silently misroute the current lane. | `validation sweep` | Do not describe an in-progress shared-contract wave as already complete. |
| 2 | `peer-runtime-warning-reduction` | reduce noisy peer/runtime warning classes without changing meaning | Atlas and Wonder peer/runtime surfaces are now materially useful, so warning output must stay readable without widening semantics. | Source Discovery, media geolocation, Statuspage, Mastodon, Stack Exchange, and seed-packet surfaces remain review or runtime infrastructure only. | `medium` because cleanup can drift into semantic changes if not bounded. | `runtime hardening` | Do not imply hidden polling, automatic approval, or live scheduler proof. |
| 3 | `peer-runtime-classification-maintenance` | keep peer/runtime labels aligned after the completed compatibility sweep | The new peer inputs are useful, but the main remaining work is label consistency rather than another fresh contract pass. | Peer/runtime slices remain below implementation or validation proof. | `medium` because wording drift can silently over-promote peer surfaces. | `governance rollout` | Do not convert peer/runtime alignment into source-validation proof. |

## Data AI

| Rank | Source or workflow id | First safe slice | Why it matters now | Source or evidence caveats | Validation or status risk | Handoff type | Do-not-do boundary |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | `data-governance-closure-on-new-wave` | keep export-safe and topic-safe composition honest over the landed institutional and internet-context wave only | The next Data risk is not missing ideas; it is over-reading new machine sources once they land. | Feed and institutional text remain untrusted/contextual. | `medium` because new source success often tempts scoring drift. | `workflow follow-on` | Do not convert new source availability into trust scoring, corroboration scoring, or action engines. |
| 2 | `data-post-institutional-consumer-or-export-closure` | keep the landed `SEC EDGAR` and `USAspending` slices bounded while recording one consumer or export-path closure only | The official institutional follow-on is already landed, so the next honest Data move is bounded follow-through rather than another fresh institutional source claim. | Institutional records remain contextual only and do not prove wrongdoing or legal status. | `medium` because institutional follow-through can quietly widen into dossier behavior. | `workflow follow-on` | Do not widen into multi-source entity dossiers or company-risk scoring. |
| 3 | `data-hdx-or-reliefweb-verification-boundary` | verification-only routing on `HDX`, `UNOSAT on HDX`, or `ReliefWeb` if Manager wants the harder user-priority follow-on lane | The remaining user-priority backlog after the landed institutional follow-on is verification-heavy, not connector-ready by default. | Public-resource posture and no-signup rules must stay explicit. | `high` because verification drift can silently over-approve a blocked source. | `verification` | Do not route catalog breadth or approval-gated APIs as assignment-ready by usefulness alone. |

## Geospatial AI

| Rank | Source or workflow id | First safe slice | Why it matters now | Source or evidence caveats | Validation or status risk | Handoff type | Do-not-do boundary |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | `geospatial-geonames-follow-on` | one bounded static enrichment slice over `GeoNames` only | The `NOAA nowCOAST` plus `National Weather Service Alerts API` wave is landed, so `GeoNames` is the cleanest remaining user-priority geospatial follow-on. | `GeoNames` is static enrichment only and must stay separate from live hazard or legal-boundary truth. | `medium` because static enrichment can silently sprawl into broader geocoding or authority claims. | `source follow-on` | Do not crowd out the landed nowCOAST/NWS wave with broad gazetteer or admin-platform scope. |
| 2 | `geospatial-nowcoast-nws-follow-through` | keep the landed `NOAA nowCOAST` and `National Weather Service Alerts API` slices honest across reporting/export follow-through only | The next Geospatial risk is flattening warning, event, and display-layer semantics once both slices exist. | Alerts, warning text, display layers, and static/reference context must stay separate. | `medium` because consumer/export flattening can hide evidence-basis differences. | `workflow follow-on` | Do not turn warning or display-layer text into damage, impact, causation, or action claims. |
| 3 | `geoboundaries-admin-follow-through` | keep geoBoundaries as bounded reference follow-through only | geoBoundaries is already implemented and should not displace the new warning/hazard source wave. | Static/reference context is not legal, operational, or live-incident truth. | `medium` because stale routing can reopen it as fake fresh work. | `workflow follow-on` | Do not widen into multi-country ingestion or treat boundaries as live event truth. |

## Marine AI

| Rank | Source or workflow id | First safe slice | Why it matters now | Source or evidence caveats | Validation or status risk | Handoff type | Do-not-do boundary |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | `marine-navtex-follow-through` | keep the landed `Navtex` slice export-safe and semantically separate over the existing marine reporting stack | The `Navtex` source wave is landed, so the next Marine risk is over-reading new warning text across summaries and exports. | Warning text remains contextual only and must not imply closure certainty, threat, or legal status. | `medium` because NAVTEX wording is easy to overread operationally. | `workflow follow-on` | Do not turn NAVTEX text into certainty, legal, threat, or action claims. |
| 2 | `marine-small-static-follow-on` | add only one compact static marine-context helper such as `GEBCO` if it remains inert and coherent | A tiny static follow-on may help the lane without reopening broad portal or viewer work. | Static context is not crisis, closure, or navigation truth. | `medium` because bathymetry/context can be over-promoted. | `source follow-on` | Do not widen into browser-map scraping or broad portal ingestion. |
| 3 | `marine-reporting-coherence-on-new-wave` | keep the new warning/context source export-safe over the existing marine reporting stack | The next Marine risk is overclaiming once a new warning source lands. | Cross-source fusion still does not prove closure, wrongdoing, escort/payment, or action need. | `medium` because new warnings can flatten caveats if not bounded. | `workflow follow-on` | Do not collapse warning/context inputs into operational conclusions. |

## Aerospace AI

| Rank | Source or workflow id | First safe slice | Why it matters now | Source or evidence caveats | Validation or status risk | Handoff type | Do-not-do boundary |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | `aerospace-gpsjam-follow-through` | keep `GPSJam` caveats and source distinctions intact across export and question surfaces only | The `GPSJam` source wave is landed, so the next Aerospace risk is flattening contextual GNSS-disruption signals into stronger operational claims. | `GPSJam` is contextual disruption awareness only and must stay separate from operational, advisory, archive, and observed sources. | `medium` because GNSS-disruption context is easy to overread into outage or threat certainty. | `workflow follow-on` | Do not infer jamming attribution, GPS failure certainty, safety consequence, or action need. |
| 2 | `aviationWeatherContext-workflow` | keep workflow closure honest on the already-built aerospace stack after the new source lands | Aerospace still has a host-specific smoke gap and should not overclaim validation even while expanding. | Observed, forecast, archive, advisory, and contextual disruption semantics must stay separate. | `high` because executed smoke is still host-blocked. | `workflow hardening` | Do not let a fresh source distract from the explicit smoke/validation gap. |
| 3 | `aerospace-export-coherence-follow-through` | keep `GPSJam` caveats and source distinctions intact across export and question surfaces only | The next Aerospace risk is flattening source distinctions once the new source is present. | Current, archive, advisory, observed, and GNSS-disruption context must remain separate. | `medium` because export flattening can hide evidence-basis differences. | `workflow follow-on` | Do not collapse contextual GNSS signals into route, target, or failure claims. |

## Features/Webcam AI

| Rank | Source or workflow id | First safe slice | Why it matters now | Source or evidence caveats | Validation or status risk | Handoff type | Do-not-do boundary |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | `features-osm-overpass-geofabrik-follow-through` | keep the landed `OpenStreetMap` / `Overpass API` / `Geofabrik` lead-support evidence bounded and candidate-only | The lead-support wave is completed source-ops evidence, so the next risk is accidental promotion of map-only leads into implementation intent. | OSM presence does not prove live camera availability or a machine-usable endpoint. | `medium` because map-only leads are easy to over-promote. | `candidate support` | Do not activate, schedule, validate, or imply live ingest from map-only leads. |
| 2 | `one-conservative-camera-candidate-only` | add at most one new conservative candidate only if the endpoint/media bar is genuinely cleared | Candidate growth remains useful, but only if it stays stricter than viewer-only discovery. | Candidate and endpoint-verified states remain pre-implementation only. | `medium` because candidate growth is easy to over-promote into activation intent. | `candidate support` | Do not scrape viewer-only apps or use browser-only tricks as source approval. |
| 3 | `finland-digitraffic` | one bounded consumer or status-classification follow-on over the implemented backend slice | This remains the cleanest already-implemented operational-context win if Features does not widen the candidate-support pass. | Keep road-weather scope separate from cameras or transport analytics sprawl. | `medium` because there is still no workflow validation record. | `consumer follow-on` | Do not widen into full transport analytics or cross-mode feed fusion. |

## Gather AI

| Rank | Source or workflow id | First safe slice | Why it matters now | Source or evidence caveats | Validation or status risk | Handoff type | Do-not-do boundary |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | `status-reconciliation-after-each-wave` | keep board, validation, workflow, prompt, and routing docs synchronized after each controlled-agent completion | Multiple lanes are moving faster than the docs, so stale status is now one of the main failure modes. | Gather owns truth reconciliation, not implementation proof. | `high` because mixed status surfaces can misroute whole waves. | `governance` | Do not promote helper or peer evidence into workflow validation. |
| 2 | `user-priority-source-list-governance` | keep the new user-supplied list explicitly bucketed into assign-now, follow-on, duplicate, discovery-only, browser-only, and safety-sensitive lanes | The current risk is classification drift, not missing ideas. | Held verification is not implementation or approval by itself. | `high` because weak bucketing can waste whole implementation lanes. | `governance packet` | Do not widen a narrow verification memo into a broad source-family approval. |
| 3 | `peer-input-governance` | keep Atlas, Wonder, Browser Use guidance, and Source Discovery runtime work represented as peer/runtime input only | Peer contributions are now materially useful, but they are easy to over-promote. | Peer and user-directed work is not Manager-owned validation proof. | `high` because governance drift can reclassify peer input as implementation truth. | `governance` | Do not convert peer docs, audits, or runtime notes into source-validation proof. |

## Immediate Manager Order

If Manager AI wants the fastest next larger wins without reopening prompt archaeology:

1. `Connect`: `connect-shared-reporting-loop-contract-wave`
2. `Data`: `data-governance-closure-on-new-wave`
3. `Geospatial`: `geospatial-geonames-follow-on`
4. `Marine`: `marine-navtex-follow-through`
5. `Aerospace`: `aerospace-gpsjam-follow-through`
6. `Features/Webcam`: `features-osm-overpass-geofabrik-follow-through`
7. `Gather`: `status-reconciliation-after-each-wave`

## Peer Input Note

- Atlas Source Discovery runtime review remains peer/runtime input only unless Connect separately validates the current runtime boundaries, and the newer Atlas runtime operator-console slice should stay in that same pending-validation posture.
- Atlas media geolocation hardening remains peer and derived-evidence input only, not source-validation proof.
- Wonder Browser Use and browser-security guidance remain verification guidance only and should inform agent behavior, not source-validation promotion.
- Wonder macOS/plugin/connector planning remains peer planning input only and should inform routing boundaries, not implementation or source-validation promotion.
- Wonder Stack Exchange and seed-packet discovery remain candidate/review discovery input only and should not be treated as source approval, implementation proof, or workflow-validation proof.
- Wonder archive-index scan, mailing-list archive adapters, and curated directory or regional-portal scan remain candidate/review/runtime discovery input only and should not be treated as source approval, implementation proof, or workflow-validation proof.
- Source Discovery breadth, seed-packet lineage, and media/OCR or geolocation runtime helpers remain candidate, review, peer, runtime, or derived-evidence input only and should not be treated as source approval, implementation proof, or workflow-validation proof.
